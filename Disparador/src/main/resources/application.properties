spring.application.name=microsservico
aws.accessKeyId=SEU_ACCESS_KEY_ID
aws.secretKey=SEU_SECRET_KEY
aws.region=us-east-1
spring.application.name=microsservico

# AWS credentials and region
# These use environment variables if present; otherwise fallback to dummy values.
aws.accessKeyId=${AWS_ACCESS_KEY_ID:dummy}
aws.secretKey=${AWS_SECRET_KEY:dummy}
aws.region=${AWS_REGION:us-east-1}

# Optional: source email (currently hardcoded in code)
# aws.ses.source-email=${AWS_SES_SOURCE_EMAIL:no-reply@example.com}

telegram:
  bot-token: 7940571849:AAErcOf5vofLpzQOGbuFPBKJVgNvGpAUpzw
  chat-id: ${TELEGRAM_CHAT_ID}
    service:
    url: http://localhost:8082
